<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">
  <meta name='viewport' content='width=device-width, initial-scale=1'>
<script src='https://kit.fontawesome.com/a076d05399.js' crossorigin='anonymous'></script>
  <title>Lista de Logins</title>
</head>
<style>
        *{
          margin: 0;
        }
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #C8A2C8;
        }

.dark-theme {
  background-color: #333; /* Cor de fundo escuro */
  color: #fff; /* Cor do texto escuro */
}

.dark-theme .video-container {
  background-color: #222; /* Cor de fundo do contêiner de vídeo escuro */
}

.dark-theme .header {
  background-color: #444; /* Cor de fundo do cabeçalho escuro */
}

.dark-theme footer {
  background-color: #444; /* Cor de fundo do cabeçalho escuro */
}


        h1 {
            color: white;
        }

        .menu-container {
            position: fixed;
            top: 0;
            left: -100%;
            height: 100%;
            width: 180px;
            background-color: #333;
            color: #fff;
            padding: 20px;
            transition: left 0.3s ease-in-out;
        }

        .menu-container.show-menu {
            left: 0;
        }

        .menu-container button {
            display: block;
            width: 100%;
            margin-bottom: 10px;
            padding: 10px;
            font-size: 16px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        .menu-container button:hover {
            background-color: #0056b3;
        }

        .menu-toggle {
            position: fixed;
            top: 10px;
            left: 10px;
            font-size: 24px;
            cursor: pointer;
            display: block;
            color: white;
        }
        
        /* Estilos para o botão de perfil no canto superior direito */
        .btn_icon_header {
            font-size: 24px;
            background-color: transparent;
            border: none;
            cursor: pointer;
            color: white;
            margin-left: auto;
        }

        .header {
            background-color: blueviolet;
            justify-content: space-between;
            padding: 0 10%;
            height: 3.5em;
            margin: 0;
            box-shadow: 1px 1px 4px black;
            display: flex;
            align-items: center; /* Centralizar itens verticalmente */
        }

        .menu-toggle.hide-menu {
            display: none;
        }

        .close-button {
            position: absolute;
            top: 10px;
            left: 10px;
            font-size: 24px;
            cursor: pointer;
        }

        .video-container {
            margin-left: 200px;
            max-width: 99%;
            margin: 20px auto;
            margin: 15px;
            background-color: #b040ff;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.2);
            padding: 20px;
            border-radius: 8px;
            color: white;
            box-shadow: 1px 1px 1px black;
        }
        input[type="text"],
        input[type="password"],
        input[type="submit"] {
            width: 90%;
            padding: 10px;
            margin: 10px 0;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 4px;
            background-color: white;
            color: black;
        }
        .video-player-container {
            margin-top: 10px;
            width: 100%;
            display: flex;
            justify-content: center;
        }
        a {
            text-decoration: none;
        }
        
        
.download-button {
    left: 10px;
}

.update-button {
    right: 10px;
}

.image-container {
    position: relative;
    width: 100%;
    max-width: 500px;
    margin: 10px;
    overflow: hidden;
}

#imageGallery {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
}

.image {
    display: block;
    width: 100%;
    height: auto;
    object-fit: cover;
    border: 4px solid #FFFFFF;
    box-sizing: border-box;
}

footer {
            background-color: blueviolet;
            color: #fff;
            padding: 10px;
            text-align: center;
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            box-shadow: 4px 1px 1px 2px black;
        }

        footer p {
            margin: 0;
        }
        footer a {
            color: var(--color-purple);
        }

/* Restante do CSS omitido por questão de concisão */

.logo_header {
  display: flex;
  align-items: center;
  margin-left: auto; /* Mover para o canto direito */
  font-size: 21px; /* Tamanho do texto AniKit */
  font-family: 'Roboto', sans-serif; /* Usando a fonte Roboto do Google Fonts */
}

/* Estilo para a imagem de perfil no botão */
.profile-image {
  width: 30px; /* Ajuste o tamanho conforme necessário */
  height: 30px; /* Ajuste o tamanho conforme necessário */
  border-radius: 50%; /* Para ter a forma circular */
  margin-right: 10px; /* Espaçamento entre a imagem e o texto */
}

/* Estilo para o ícone do usuário do Font Awesome */
.btn_icon_header {
  font-family: 'Font Awesome 5 Free'; /* Usando a fonte Font Awesome */
  font-size: 21px;
  margin-left: auto; 
}

 
 i {
 color: white;
 }
    </style>
</head>




<body>

  <div class="menu-container" id="menu">
        <span class="close-button" onclick="toggleMenu()">&#10006;</span>
        <h3>Menu</h3><br><hr><br>
        <button onclick="toggleDarkTheme()">Tema Escuro</button>
        <button onclick="changeBackgroundColor()">Mudar Cor</button>
  </div>
  <div class="header">
    <div class="menu-toggle" onclick="toggleMenu()">&#9776;</div>
    <div class="logo_header">
      <i>AniKit   </i>
      <button onclick="redirectToProfile()" class="btn_icon_header">
        <i class='fas fa-user-edit'>
</i>
      </button>
    </div>
  </div>
    <br>
    <h1>Transformando imagens de anime em realidade.</h1>

    <div id="imageGallery"></div><br><br><br>

    <footer>
      <p>Desenvolvido com 💜 por <a href="https://youtube.com/@clovermods">@clovermods</a></p>
   </footer>

    <script>

    // ... outros códigos ...

    function getURLParams() {
            const searchParams = new URLSearchParams(window.location.search);
            const username = searchParams.get('username');
            const key = searchParams.get('key');
            return { username, key };
        }

        // Função para redirecionar o usuário para o perfil com a chave e o nome do usuário
        function redirectToProfile() {
            const { username, key } = getURLParams();
            if (username && key) {
                const profileUrl = `/perfil/${username}?key=${key}`;
                window.location.href = profileUrl;
            } else {
                alert('Nome de usuário ou chave não fornecidos na URL.');
            }
        }

    // ... outros códigos ...
      // Função para atualizar a imagem
function updateImage() {
    // Fazer uma solicitação para a API de imagens de animes
    fetch('https://appp--trevodev.repl.co/anime/elaina?username=SUPREMO&key=SER_SUPREMO')
        .then(response => response.json())
        .then(data => {
            const imageGallery = document.getElementById('imageGallery');
            imageGallery.innerHTML = ''; // Limpar as imagens anteriores

            // Verificar se a resposta contém a propriedade 'url'
            if (data.url) {
                // Criar elemento de imagem e definir a URL da imagem
                const imgElement = document.createElement('img');
                imgElement.src = data.url;
                imgElement.classList.add('image');

                // Criar contêiner para a imagem
                const imageContainer = document.createElement('div');
                imageContainer.classList.add('image-container');

                // Adicionar a imagem ao contêiner
                imageContainer.appendChild(imgElement);

 // Criar botão de download
const downloadButton = document.createElement('button');
downloadButton.innerHTML = '<i class="fas fa-download"></i>';
downloadButton.classList.add('download-button');
downloadButton.addEventListener('click', function() {
    // Lógica para download da imagem
    const link = document.createElement('a');
    link.href = data.url;
    link.download = 'imagem.jpg';
    link.click();
});

// Adicionar o botão de download ao contêiner
imageContainer.appendChild(downloadButton);


                // Criar botão de atualização
                const updateButton = document.createElement('button');
                updateButton.innerHTML = '<i class="fas fa-sync-alt"></i>';
                updateButton.classList.add('update-button');
                updateButton.addEventListener('click', updateImage);

                // Adicionar o botão de atualização ao contêiner
                imageContainer.appendChild(updateButton);

                // Adicionar o contêiner de imagem à galeria
                imageGallery.appendChild(imageContainer);
            }
        })
        .catch(error => console.log(error));
}

// Chamar a função para exibir a primeira imagem
updateImage();
//setInterval(updateImage, 5000);

        function toggleDarkTheme() {
                        const body = document.querySelector('body');
            const card = document.querySelector('.video-container ');
            const cabeca = document.querySelector('.header');
            const footer = document.querySelector('footer');
            body.classList.toggle('dark-theme');
            cabeca.classList.toggle('dark-theme');
            card.classList.toggle('dark-theme');
            footer.classList.toggle('dark-theme');
       
        }

        function changeBackgroundColor() {
            const newColor = prompt('Digite uma cor em formato hexadecimal (#RRGGBB) ou um nome de cor:');
            const body = document.querySelector('body');
            body.style.backgroundColor = newColor;
        }

        

        function toggleMenu() {
            const menu = document.getElementById('menu');
            const menuToggle = document.querySelector('.menu-toggle');

            menu.classList.toggle('show-menu');
            menuToggle.classList.toggle('hide-menu');
        }
    </script>
</body>
</html>
